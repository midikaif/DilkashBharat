<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Places</title>
  </head>
  <body>
    <h1>All Places</h1>
    <p><a href="/places/new">Add new place</a></p>
    <ul>
      <% for(let place of allPlaces){ %>
      <li><a href="/places/<%= place._id %>"><%= place.title %></a></li>
      <% } %>
    </ul>
  </body>
</html>
